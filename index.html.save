<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Official Website — SN DESIGN STUDIO</title>

    <meta name="description" content="Create viral AI content, CapCut templates and creator growth systems with SN Design Studio.">
    <meta name="keywords" content="AI creator, AI video, AI content creation, CapCut AI, AI marketing system, viral AI video, SN Design Studio">

    <meta property="og:title" content="SN Design Studio | AI Creator Ecosystem">
    <meta property="og:description" content="Build viral AI content faster with cinematic creator systems.">
    <meta property="og:image" content="assets/images/hero.png">
    <meta property="og:type" content="website">

    <link rel="preload" as="image" href="assets/images/hero.png">
    <link rel="stylesheet" href="assets/css/master.css">

    <style>
        @media (max-width: 768px) {
            .ultra-system-panel {
                width: 92% !important;
                max-width: 380px !important;
                margin: 20px auto !important;
                padding: 15px !important;
                box-sizing: border-box;
            }
            .system-row {
                font-size: 13px !important;
                padding: 8px 0 !important;
            }
            .system-label {
                font-size: 11px !important;
                letter-spacing: 1px;
            }
        }
        /* คุมสัดส่วน Panel ให้ดูเป็นระเบียบทุกหน้าจอ */
        .ultra-system-panel {
            display: block;
            overflow: hidden;
            background: rgba(10, 10, 15, 0.9) !important;
            border: 1px solid rgba(0, 255, 157, 0.3) !important;
            box-shadow: 0 0 30px rgba(0, 255, 157, 0.1) !important;
        }

        /* [NEW] ส่วนเสริมสำหรับแสดงลิสต์ PM2 ให้พรีเมี่ยม */
        .service-list-container {
            margin-top: 15px;
            padding-top: 10px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
        .service-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 6px 0;
            font-size: 12px;
            border-bottom: 1px shadow rgba(255, 255, 255, 0.05);
        }
        .service-name {
            color: #aaa;
            font-family: 'Courier New', monospace;
        }
        .service-status-tag {
            font-weight: bold;
            letter-spacing: 1px;
            text-shadow: 0 0 5px currentColor;
        }
        .status-online { color: #00ff9d; }
        .status-offline { color: #ff4d4d; }
    </style>
</head>

<body class="index-page">

    <div id="nav"></div>

    <section class="marquee-wrap">
        <div class="marquee-text">
            แพลตฟอร์ม AI สำหรับครีเอเตอร์ไทย • สร้างภาพ AI • Copy ใบหน้า • Copy ท่าเต้น • CapCut Template • SEO Marketing • Creator Growth System
        </div>
    </section>

    <section class="hero-master">
        <img src="assets/images/hero.png" alt="SN Design Studio AI creator ecosystem cinematic power red branding" loading="eager">
        <div class="hero-content-bottom fade-up">
            <h1>Build Viral AI Content Faster</h1>
            <p>Dark Creator Ecosystem for AI Video.</p>
        </div>
    </section>

    <section class="ultra-gui-preview ultra-dashboard">
        <div class="gui-container">
            <div class="gui-mode">
                AI PREVIEW MODE — SN DESIGN STUDIO
            </div>

            <div class="gui-screen">
                <img src="assets/images/sn-design-studio-ultra-ai-preview-dashboard.png">
                <div class="scan-line"></div>
            </div>

            <div class="ultra-system-panel ultra-enterprise">
                <div class="system-title">
                    ULTRA ENTERPRISE STATUS
                </div>

                <div class="system-row">
                    <div class="system-label">Server</div>
                    <div class="system-state online">
                        <span class="dot"></span>
                        <span id="server-status">CONNECTING...</span>
                    </div>
                </div>

                <div class="system-row">
                    <div class="system-label">CPU Load</div>
                    <div class="system-state online">
                        <span class="dot"></span>
                        <span id="cpu-load">---</span>
                    </div>
                </div>

                <div class="system-row">
                    <div class="system-label">RAM Available</div>
                    <div class="system-state online">
                        <span class="dot"></span>
                        <span id="ram-usage">---</span>
                    </div>
                </div>

                <div class="system-row">
                    <div class="system-label">Active Users</div>
                    <div class="system-state online">
                        <span class="dot"></span>
                        <span id="user-count">0</span>
                    </div>
                </div>

                <div class="service-list-container" id="pm2-container">
                    <div style="font-size: 10px; color: #555; margin-bottom: 5px; letter-spacing: 2px;">ACTIVE SERVICES</div>
                    <div id="pm2-services-list">
                        <div class="service-item"><span class="service-name">Initializing...</span></div>
                    </div>
                </div>

                <div class="system-row" style="margin-top: 10px; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 10px;">
                    <div class="system-label">AI Engine</div>
                    <div class="system-state online">
                        <span class="dot"></span>
                        <span id="ai-status">WAITING</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="ultra-intro-zone">
        <div class="container">
            <div class="intro-cinematic">
                <h2 class="intro-title">SN Design Studio — AI Power System</h2>
                <p class="intro-text">
                    แพลตฟอร์ม AI สำหรับครีเอเตอร์ไทย • สร้างภาพ AI • Copy ใบหน้า • Copy ท่าเต้น • CapCut Template • SEO Marketing • Creator Growth System
                </p>
            </div>
            <div class="intro-video">
                <iframe src="https://www.youtube.com/embed/NI7WALIXStU" title="YouTube Shorts" frameborder="0" allowfullscreen></iframe>
            </div>
        </div>
    </section>

    <footer>
        © SN Design Studio, 2026 · All rights reserved.
    </footer>

    <script src="assets/js/nav.js"></script>

    <script>
        document.querySelectorAll("[data-template]").forEach(el => {
            el.addEventListener("click", () => {
                const model = el.dataset.template;
                if (model) {
                    window.location.href = "create.html?template=" + model;
                }
            });
        });
    </script>

    <script>
        const BASE_API = "https://api.sn-designstudio.dev";
        const SYSTEM_API = `${BASE_API}/api/admin/system-status`;
        const REALTIME_API = `${BASE_API}/api/admin/realtime-status`;

        async function updateStatus() {
            try {
                // 1. ดึงข้อมูล VPS (โครงสร้างเดิม)
                const resSystem = await fetch(SYSTEM_API);
                const dataSystem = await resSystem.json();

                if (dataSystem.success) {
                    const m = dataSystem.monitor;
                    document.getElementById("cpu-load").innerText = m.cpu + "%";
                    document.getElementById("ram-usage").innerText = m.ramUsed + " GB"; // หมายเหตุ: แก้ไขตาม Backend ที่ส่งมา (Available)
                    document.getElementById("user-count").innerText = m.users;
                    document.getElementById("server-status").innerText = "ULTRA ONLINE";
                    document.getElementById("ai-status").innerText = "CONNECTED";
                }

                // 2. ดึงข้อมูล PM2 Services (ส่วนที่เพิ่มใหม่)
                // หมายเหตุ: เนื่องจากมี authMiddleware หากต้องการให้คนทั่วไปเห็น พี่อาจต้องปรับที่ Backend ให้ Route นี้เป็น Public
                const resRealtime = await fetch(REALTIME_API);
                const dataRealtime = await resRealtime.json();

                if (dataRealtime.success) {
                    const listEl = document.getElementById("pm2-services-list");
                    listEl.innerHTML = dataRealtime.services.map(s => `
                        <div class="service-item">
                            <span class="service-name">${s.name}</span>
                            <span class="service-status-tag ${s.status === 'online' ? 'status-online' : 'status-offline'}">
                                ${s.status === 'online' ? '● ONLINE' : '○ OFF'}
                            </span>
                        </div>
                    `).join('');
                }

            } catch (e) {
                console.log("STATUS SYNC ERROR", e);
                document.getElementById("server-status").innerText = "OFFLINE";
                document.getElementById("ai-status").innerText = "DISCONNECTED";
            }
        }

        // อัปเดตทุก 5 วินาที
        setInterval(updateStatus, 5000);
        updateStatus();
    </script>

</body>
</html>

